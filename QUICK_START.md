# 🚀 快速部署指南

## 一键部署命令

### 步骤 1：上传项目到服务器

```bash
# 在本地终端执行（替换 user 为你的服务器用户名）
scp -r /Users/balabibo/Jobs/Study/dabo_personal user@47.110.87.81:/var/www/
```

### 步骤 2：SSH 登录并执行部署

```bash
# SSH 登录服务器
ssh user@47.110.87.81

# 进入项目目录
cd /var/www/dabo_personal

# 赋予执行权限
chmod +x deploy.sh

# 执行自动部署（首次部署）
./deploy.sh --setup
```

### 步骤 3：访问网站

部署完成后访问：
- **Next.js 应用**：`http://47.110.87.81:3000`
- **Nginx（如已配置）**：`http://47.110.87.81`

---

## 后续更新部署

```bash
# SSH 登录服务器
ssh user@47.110.87.81

# 进入项目目录并更新
cd /var/www/dabo_personal

# 执行更新部署
./deploy.sh --update
```

---

## 常用命令

```bash
# 查看应用状态
pm2 status

# 查看实时日志
pm2 logs dabo-personal

# 重启应用
pm2 restart dabo-personal

# 停止应用
pm2 stop dabo-personal
```

---

## 故障排查

### 应用无法访问

```bash
# 检查应用是否运行
pm2 status

# 检查端口是否监听
netstat -tuln | grep 3000

# 查看错误日志
pm2 logs dabo-personal --err
```

### 数据库连接失败

```bash
# 检查环境变量
cat /var/www/dabo_personal/.env.local

# 测试数据库连接
mysql -h 47.110.87.81 -P 3306 -u admin -p personal_web
```

---

## 📚 详细文档

- **完整部署文档**：[DEPLOYMENT.md](file:///Users/balabibo/Jobs/Study/dabo_personal/DEPLOYMENT.md)
- **部署计划**：查看 artifacts 中的 `implementation_plan.md`
- **完成总结**：查看 artifacts 中的 `walkthrough.md`

---

## ⚠️ 注意事项

1. **防火墙**：确保服务器防火墙开放了 3000 端口（或 80/443 端口）
2. **数据库**：确认数据库 `47.110.87.81:3306` 可访问
3. **权限**：确保有足够的权限在 `/var/www` 目录创建文件
4. **Node.js**：脚本会自动安装 Node.js 20.x，无需手动安装

---

## 🎯 下一步优化

部署成功后，建议：
1. 配置域名和 SSL 证书（HTTPS）
2. 设置数据库定期备份
3. 配置监控和告警
4. 优化静态资源（CDN）
